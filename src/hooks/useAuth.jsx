/* import { useContext } from "react";
import { AuthContext } from "../js/contexts/authContext"; // Ensure this path is correct

export const useAuth = () => {
  return useContext(AuthContext);
};
 */
